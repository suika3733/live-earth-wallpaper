## v3.1.0 — 修复「自动刷新后自动设为壁纸」机制

### 新增/修复功能

- **修复自动刷新后无法自动设为壁纸** — 应用启动即在后端启动常驻调度器，按当前选中的数据源定时自动刷新并设为壁纸，不再依赖前端窗口打开
- **「自动刷新」与「自动设为壁纸」独立开关** — 可分别控制卫星、SDO、APOD 三种数据源是否自动刷新、刷新后是否自动设为壁纸
- **真实倒计时同步** — 前端倒计时改为基于后端下次刷新时间，与调度器实际刷新时机一致
- **切换数据源即时生效** — 界面切换 APOD / 卫星 / SDO 时同步写入后端配置，调度器自动跟随

### 涉及数据源

| 数据源 | 自动刷新间隔 | 自动设壁纸开关 |
|--------|--------------|----------------|
| NASA APOD | 每日（可设时间） | `apod_auto_set_wallpaper` |
| 卫星影像 | 可配置（默认 10 分钟） | `sat_auto_set_wallpaper` |
| SDO 太阳观测 | 可配置（默认 60 分钟） | `sdo_auto_set_wallpaper` |

---

## v1.5 — 壁纸水印 + 后台运行

### 新增功能

- **壁纸水印角标** — 每张设为壁纸的图片右上角自动标注来源和拍摄时间，半透明圆角设计，不遮挡主画面
  - NASA APOD：`来源: NASA 每日天文图片 (APOD)` + `拍摄: 日期 | 标题`
  - Himawari-8：`来源: Himawari-8 气象卫星` + `拍摄时间: YYYY-MM-DD HH:MM (UTC+8)`
- **最小化到任务栏** — 点击关闭按钮弹出选择对话框，可选择最小化到后台持续更新壁纸，或完全退出程序

### 优化改进

- GUI 窗口扩大至 1300x850，图片预览区域更大
- Himawari-8 地球图分辨率升级至 **2200x2200**（4x4=16 瓦片拼接）
- 地球模式每 10 分钟自动刷新，界面显示倒计时
- 首次启动自动拉取近 10 天 NASA APOD 图片，免去手动获取

### 发布文件

| 文件 | 说明 |
|------|------|
| `NASA_Wallpaper.exe` | 独立可执行文件，无需 Python 环境，双击运行 |
| `使用说明.txt` | 中文使用说明 |

### 系统要求

- Windows 10 / 11
- 首次启动需要网络连接（获取 NASA/Himawari-8 数据）

### 使用方式

1. 下载 `NASA_Wallpaper_v1.5.zip`
2. 解压到任意目录
3. 双击 `NASA_Wallpaper.exe` 运行
